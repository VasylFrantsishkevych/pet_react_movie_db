import React, {FC} from 'react';

import {MoviesList} from "../../components";

const MoviePage: FC = () => {
    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export {MoviePage}
